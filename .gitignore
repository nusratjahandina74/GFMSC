backend/node_modules/
frontend/node_modules/
backend/dist/
frontend/dist/

# Never commit real secrets. .env.example (no real values) is fine to
# commit; .env, .env.local, .env.production.local etc are not.

