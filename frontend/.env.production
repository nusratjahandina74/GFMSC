# Used automatically by Vite when running `vite build` (production build),
# which is what Vercel runs. This ensures the deployed site talks to the
# real backend instead of localhost.
# Replace with your actual Render/production backend URL if different.
VITE_API_BASE_URL=https://gfmsc-backend.onrender.com/api
